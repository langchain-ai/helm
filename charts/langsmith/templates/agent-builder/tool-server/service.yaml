{{- if and .Values.config.agentBuilder.enabled .Values.agentBuilderToolServer.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.agentBuilderToolServer.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.agentBuilderToolServer.name }}
    {{- with .Values.agentBuilderToolServer.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.agentBuilderToolServer.service.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.agentBuilderToolServer.service.type }}
  {{- with .Values.agentBuilderToolServer.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if .Values.agentBuilderToolServer.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.agentBuilderToolServer.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.agentBuilderToolServer.service.port }}
      targetPort: {{ .Values.agentBuilderToolServer.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "langsmith.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ include "langsmith.fullname" . }}-{{ .Values.agentBuilderToolServer.name }}
{{- end }}

