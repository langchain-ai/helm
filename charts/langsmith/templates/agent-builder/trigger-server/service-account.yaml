{{- if and .Values.agentBuilder.enabled .Values.agentBuilder.triggerServer.enabled .Values.agentBuilder.triggerServer.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agentBuilderTriggerServer.serviceAccountName" . }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.agentBuilder.triggerServer.name }}
    {{- with .Values.agentBuilder.triggerServer.serviceAccount.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.agentBuilder.triggerServer.serviceAccount.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
automountServiceAccountToken: {{ .Values.agentBuilder.triggerServer.serviceAccount.automountServiceAccountToken }}
{{- end }}

