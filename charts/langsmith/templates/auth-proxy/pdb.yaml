{{- if and .Values.authProxy.enabled .Values.authProxy.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.authProxy.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.authProxy.pdb.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.authProxy.pdb.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ include "langsmith.fullname" . }}-{{ .Values.authProxy.name }}
  {{- if .Values.authProxy.pdb.minAvailable }}
  minAvailable: {{ .Values.authProxy.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.authProxy.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.authProxy.pdb.maxUnavailable }}
  {{- end }}
{{- end }}
