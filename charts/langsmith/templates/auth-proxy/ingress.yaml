{{- if and .Values.authProxy.enabled .Values.authProxy.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.authProxy.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.authProxy.ingress.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.authProxy.ingress.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- with .Values.authProxy.ingress.ingressClassName }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- with .Values.authProxy.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
  {{- range .Values.authProxy.ingress.hosts }}
  - host: {{ .host }}
    http:
      paths:
      {{- range .paths }}
      - path: {{ .path | default "/" }}
        pathType: {{ .pathType | default "Prefix" }}
        backend:
          service:
            name: {{ include "langsmith.fullname" $ }}-{{ $.Values.authProxy.name }}
            port:
              number: {{ $.Values.authProxy.service.port }}
      {{- end }}
  {{- end }}
{{- end }}
