{{- if .Values.polly.enabled }}
{{- if not .Values.polly.redis.external.enabled }}
{{- $volumes := concat .Values.commonVolumes .Values.polly.redis.deployment.volumes -}}
{{- $volumeMounts := concat .Values.commonVolumeMounts .Values.polly.redis.deployment.volumeMounts -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "polly") }}-{{ .Values.polly.redis.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.polly.redis.deployment.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.polly.redis.deployment.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "polly") }}-{{ .Values.polly.redis.name }}
  template:
    metadata:
      {{- with .Values.polly.redis.deployment.annotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- with .Values.polly.redis.deployment.labels }}
            {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "langsmith.labels" . | nindent 8 }}
        app.kubernetes.io/component: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "polly") }}-{{ .Values.polly.redis.name }}
    spec:
      {{- with .Values.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.polly.redis.deployment.priorityClassName }}
      priorityClassName: {{ .Values.polly.redis.deployment.priorityClassName }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.polly.redis.deployment.terminationGracePeriodSeconds }}
      securityContext:
        {{- toYaml .Values.polly.redis.deployment.podSecurityContext | nindent 8 }}
      serviceAccountName: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "polly") }}-{{ .Values.polly.redis.name }}
      {{- include "langsmith.dnsConfig" . | nindent 6 }}
      containers:
        - name: {{ .Values.polly.redis.name }}
          {{- with .Values.polly.redis.deployment.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if .Values.polly.redis.deployment.extraEnv }}
          env:
            {{- with .Values.polly.redis.deployment.extraEnv}}
             {{- toYaml . | nindent 12 }}
            {{- end }}
           {{- end }}
          image: {{ include "langsmith.image" (dict "Values" .Values "Chart" .Chart "component" "redisImage") | quote }}
          imagePullPolicy: {{ .Values.images.redisImage.pullPolicy }}
          ports:
            - name: redis
              containerPort: {{ .Values.polly.redis.containerPort }}
              protocol: TCP
          {{- with .Values.polly.redis.deployment.startupProbe }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.polly.redis.deployment.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.polly.redis.deployment.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.polly.redis.deployment.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.polly.redis.deployment.securityContext | nindent 12 }}
          {{- with .Values.polly.redis.deployment.lifecycle }}
          lifecycle:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        {{- with .Values.polly.redis.deployment.extraContainerConfig }}
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.polly.redis.deployment.sidecars }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.polly.redis.deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.polly.redis.deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.polly.redis.deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $volumes }}
      volumes:
          {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
{{- end }}
