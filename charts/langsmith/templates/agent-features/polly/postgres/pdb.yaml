{{- if .Values.polly.enabled }}
{{- if .Values.polly.postgres.pdb.enabled }}
{{- $componentSuffix := printf "%s-%s" .Values.polly.namePrefix .Values.polly.postgres.name -}}
{{- $componentPrefixMaxLen := int (sub 62 (len $componentSuffix)) -}}
{{- $componentPrefix := include "langsmith.fullname" . | trunc $componentPrefixMaxLen | trimSuffix "-" -}}
{{- $componentName := printf "%s-%s" $componentPrefix $componentSuffix -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ $componentName }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ $componentName }}
  {{- if .Values.polly.postgres.pdb.minAvailable }}
  minAvailable: {{ .Values.polly.postgres.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.polly.postgres.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.polly.postgres.pdb.maxUnavailable }}
  {{- end }}
{{- end }}
{{- end }}
