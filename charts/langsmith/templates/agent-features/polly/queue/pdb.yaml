{{- if .Values.polly.enabled }}
{{- if .Values.polly.queue.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "polly") }}-{{ .Values.polly.queue.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "polly") }}-{{ .Values.polly.queue.name }}
  {{- if .Values.polly.queue.pdb.minAvailable }}
  minAvailable: {{ .Values.polly.queue.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.polly.queue.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.polly.queue.pdb.maxUnavailable }}
  {{- end }}
{{- end }}
{{- end }}
