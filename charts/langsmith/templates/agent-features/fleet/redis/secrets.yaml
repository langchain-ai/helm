{{- if .Values.fleet.enabled }}
{{- if not .Values.fleet.redis.external.existingSecretName }}
{{- $componentName := include "langsmith.fleet.redisComponentName" . -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "langsmith.agentFeatures.redisSecretName" (dict "root" . "product" "fleet") }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
data:
  {{- if .Values.fleet.redis.external.enabled }}
  redis_connection_url: {{ .Values.fleet.redis.external.connectionUrl | b64enc }}
  {{- else }}
  redis_connection_url: {{ printf "redis://%s.%s.svc.%s:%v" $componentName (default .Release.Namespace .Values.namespace) .Values.clusterDomain (toString .Values.fleet.redis.service.port) | b64enc }}
  {{- end}}
{{- end }}
{{- end }}
