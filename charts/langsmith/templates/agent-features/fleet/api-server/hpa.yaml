{{- if .Values.fleet.enabled }}
{{- if and .Values.fleet.apiServer.autoscaling.enabled (not .Values.fleet.apiServer.autoscaling.keda.enabled) }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "fleet") }}-{{ .Values.fleet.apiServer.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "fleet") }}-{{ .Values.fleet.apiServer.name }}
  minReplicas: {{ .Values.fleet.apiServer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.fleet.apiServer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.fleet.apiServer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.fleet.apiServer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.fleet.apiServer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.fleet.apiServer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
{{- end }}
