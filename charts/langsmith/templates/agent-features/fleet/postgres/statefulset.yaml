{{- if .Values.fleet.enabled }}
{{- if not .Values.fleet.postgres.external.enabled }}
{{- $volumes := concat .Values.commonVolumes .Values.fleet.postgres.statefulSet.volumes -}}
{{- $volumeMounts := concat .Values.commonVolumeMounts .Values.fleet.postgres.statefulSet.volumeMounts -}}
{{- $componentSuffix := printf "%s-%s" .Values.fleet.namePrefix .Values.fleet.postgres.name -}}
{{- $componentPrefixMaxLen := int (sub 62 (len $componentSuffix)) -}}
{{- $componentPrefix := include "langsmith.fullname" . | trunc $componentPrefixMaxLen | trimSuffix "-" -}}
{{- $componentName := printf "%s-%s" $componentPrefix $componentSuffix -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ $componentName }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.fleet.postgres.statefulSet.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.fleet.postgres.statefulSet.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  serviceName: {{ $componentName }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ $componentName }}
  template:
    metadata:
      {{- with .Values.fleet.postgres.statefulSet.annotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- with .Values.fleet.postgres.statefulSet.labels }}
            {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "langsmith.labels" . | nindent 8 }}
        app.kubernetes.io/component: {{ $componentName }}
    spec:
      {{- with .Values.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.fleet.postgres.statefulSet.priorityClassName }}
      priorityClassName: {{ .Values.fleet.postgres.statefulSet.priorityClassName }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.fleet.postgres.statefulSet.terminationGracePeriodSeconds }}
      securityContext:
        {{- toYaml .Values.fleet.postgres.statefulSet.podSecurityContext | nindent 8 }}
      serviceAccountName: {{ $componentName }}
      {{- include "langsmith.dnsConfig" . | nindent 6 }}
      containers:
        - name: {{ .Values.fleet.postgres.name }}
          {{- with .Values.fleet.postgres.statefulSet.command }}
          command:
             {{ . }}
          {{- end }}
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.agentFeatures.postgresSecretName" (dict "root" . "product" "fleet") }}
                  key: postgres_db
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.agentFeatures.postgresSecretName" (dict "root" . "product" "fleet") }}
                  key: postgres_password
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.agentFeatures.postgresSecretName" (dict "root" . "product" "fleet") }}
                  key: postgres_user
            - name: PGDATA
              value: /var/lib/postgresql/data/postgres
            {{- with .Values.fleet.postgres.statefulSet.extraEnv }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
          image: {{ include "langsmith.image" (dict "Values" .Values "Chart" .Chart "component" "postgresImage") | quote }}
          imagePullPolicy: {{ .Values.images.postgresImage.pullPolicy }}
          ports:
            - name: postgres
              containerPort: {{ .Values.fleet.postgres.containerPort }}
              protocol: TCP
          startupProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -d postgres -U postgres
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -d postgres -U postgres
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -d postgres -U postgres
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          resources:
            {{- toYaml .Values.fleet.postgres.statefulSet.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.fleet.postgres.statefulSet.securityContext | nindent 12 }}
          {{- with .Values.fleet.postgres.statefulSet.lifecycle }}
          lifecycle:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- with $volumeMounts }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.fleet.postgres.statefulSet.persistence.enabled }}
            - mountPath: /var/lib/postgresql/data
              name: data
              subPath: postgres
            {{- end }}
          {{- with .Values.fleet.postgres.statefulSet.extraContainerConfig }}
            {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.fleet.postgres.statefulSet.sidecars }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.fleet.postgres.statefulSet.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.fleet.postgres.statefulSet.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.fleet.postgres.statefulSet.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $volumes }}
      volumes:
          {{- toYaml . | nindent 8 }}
      {{- end }}
 {{- if .Values.fleet.postgres.statefulSet.persistence.enabled }}
  {{- with .Values.fleet.postgres.statefulSet.persistentVolumeClaimRetentionPolicy }}
  persistentVolumeClaimRetentionPolicy:
        {{- toYaml . | nindent 4 }}
  {{- end }}
  volumeClaimTemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: data
        labels:
          {{- include "langsmith.selectorLabels" . | nindent 10 }}
          app.kubernetes.io/component: {{ $componentName }}
      spec:

        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.fleet.postgres.statefulSet.persistence.size }}
        {{- if .Values.fleet.postgres.statefulSet.persistence.storageClassName }}
        storageClassName: {{ .Values.fleet.postgres.statefulSet.persistence.storageClassName }}
        {{- end }}
  {{- end }}
{{- end}}
{{- end }}
