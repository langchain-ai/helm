{{- if .Values.fleet.enabled }}
{{- if .Values.fleet.queue.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "fleet") }}-{{ .Values.fleet.queue.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "fleet") }}-{{ .Values.fleet.queue.name }}
  {{- if .Values.fleet.queue.pdb.minAvailable }}
  minAvailable: {{ .Values.fleet.queue.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.fleet.queue.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.fleet.queue.pdb.maxUnavailable }}
  {{- end }}
{{- end }}
{{- end }}
