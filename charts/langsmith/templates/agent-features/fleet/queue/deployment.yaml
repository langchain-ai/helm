{{- if .Values.fleet.enabled }}
{{- if .Values.fleet.queue.enabled -}}
{{- $volumes := concat .Values.commonVolumes .Values.fleet.queue.deployment.volumes -}}
{{- $volumeMounts := concat .Values.commonVolumeMounts .Values.fleet.queue.deployment.volumeMounts -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "fleet") }}-{{ .Values.fleet.queue.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.fleet.queue.deployment.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.fleet.queue.deployment.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if not .Values.fleet.queue.autoscaling.enabled }}
  replicas: {{ .Values.fleet.queue.deployment.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "fleet") }}-{{ .Values.fleet.queue.name }}
  template:
    metadata:
      {{- with .Values.fleet.queue.deployment.annotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- with .Values.fleet.queue.deployment.labels }}
            {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "langsmith.labels" . | nindent 8 }}
        app.kubernetes.io/component: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "fleet") }}-{{ .Values.fleet.queue.name }}
    spec:
      serviceAccountName: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "fleet") }}-{{ .Values.fleet.queue.name }}
      {{- include "langsmith.dnsConfig" . | nindent 6 }}
      {{- with .Values.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.fleet.queue.deployment.priorityClassName }}
      priorityClassName: {{ .Values.fleet.queue.deployment.priorityClassName }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.fleet.queue.deployment.terminationGracePeriodSeconds }}
      securityContext:
        {{- merge .Values.fleet.queue.deployment.podSecurityContext (.Values.commonPodSecurityContext | default dict) | toYaml | nindent 8 }}
      {{- with .Values.commonInitContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Values.fleet.queue.name }}
          command:
            - "/storage/queue_entrypoint.sh"
          env:
            {{- with .Values.commonEnv }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            - name: PORT
              value: {{ .Values.fleet.queue.containerPort | quote }}
            - name: POSTGRES_URI
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.agentFeatures.postgresSecretName" (dict "root" . "product" "fleet") }}
                  key: postgres_connection_url
            - name: REDIS_URI
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.agentFeatures.redisSecretName" (dict "root" . "product" "fleet") }}
                  key: redis_connection_url
            - name: LANGGRAPH_CLOUD_LICENSE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.secretsName" . }}
                  key: langsmith_license_key
                  optional: true
            - name: N_JOBS_PER_WORKER
              value: {{ .Values.fleet.queue.numberOfJobsPerWorker | quote }}
            {{- with include "langsmith.fleet.extraEnv" . | trim }}
              {{- . | nindent 12 }}
            {{- end }}
          {{- with .Values.fleet.queue.deployment.envFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: {{ include "langsmith.image" (dict "Values" .Values "Chart" .Chart "component" "agentBuilderImage") | quote }}
          imagePullPolicy: {{ .Values.images.agentBuilderImage.pullPolicy }}
          ports:
            - name: queue
              containerPort: {{ .Values.fleet.queue.containerPort }}
              protocol: TCP
          startupProbe:
            {{- toYaml .Values.fleet.queue.deployment.startupProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.fleet.queue.deployment.readinessProbe | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.fleet.queue.deployment.livenessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.fleet.queue.deployment.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.fleet.queue.deployment.securityContext | nindent 12 }}
          {{- with .Values.fleet.queue.deployment.lifecycle }}
          lifecycle:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $volumeMounts }}
          volumeMounts:
              {{- toYaml . | nindent 12 }}
          {{- end }}
        {{- with .Values.fleet.queue.deployment.sidecars }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.fleet.queue.deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.fleet.queue.deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.fleet.queue.deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $volumes }}
      volumes:
          {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end}}
{{- end }}
