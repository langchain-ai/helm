{{- if or .Values.insights.enabled .Values.engine.enabled }}
{{- if not .Values.engineInsightsAgent.postgres.external.enabled }}
{{- $volumes := concat .Values.commonVolumes .Values.engineInsightsAgent.postgres.statefulSet.volumes -}}
{{- $volumeMounts := concat .Values.commonVolumeMounts .Values.engineInsightsAgent.postgres.statefulSet.volumeMounts -}}
{{- $componentName := include "langsmith.agentFeatures.componentName" (dict "root" . "product" "engineInsightsAgent" "component" "postgres") -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ $componentName }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.engineInsightsAgent.postgres.statefulSet.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.engineInsightsAgent.postgres.statefulSet.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  serviceName: {{ $componentName }}
  replicas: 1
  {{- with .Values.engineInsightsAgent.postgres.statefulSet.updateStrategy }}
  updateStrategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ $componentName }}
  template:
    metadata:
      {{- with .Values.engineInsightsAgent.postgres.statefulSet.annotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- with .Values.engineInsightsAgent.postgres.statefulSet.labels }}
            {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "langsmith.labels" . | nindent 8 }}
        app.kubernetes.io/component: {{ $componentName }}
    spec:
      {{- with .Values.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.engineInsightsAgent.postgres.statefulSet.priorityClassName }}
      priorityClassName: {{ .Values.engineInsightsAgent.postgres.statefulSet.priorityClassName }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.engineInsightsAgent.postgres.statefulSet.terminationGracePeriodSeconds }}
      securityContext:
        {{- toYaml .Values.engineInsightsAgent.postgres.statefulSet.podSecurityContext | nindent 8 }}
      serviceAccountName: {{ $componentName }}
      {{- include "langsmith.dnsConfig" . | nindent 6 }}
      containers:
        - name: {{ .Values.engineInsightsAgent.postgres.name }}
          {{- with .Values.engineInsightsAgent.postgres.statefulSet.command }}
          command:
             {{ . }}
          {{- end }}
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.agentFeatures.postgresSecretName" (dict "root" . "product" "engineInsightsAgent") }}
                  key: postgres_db
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.agentFeatures.postgresSecretName" (dict "root" . "product" "engineInsightsAgent") }}
                  key: postgres_password
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "langsmith.agentFeatures.postgresSecretName" (dict "root" . "product" "engineInsightsAgent") }}
                  key: postgres_user
            - name: PGDATA
              value: /var/lib/postgresql/data/postgres
            {{- with .Values.engineInsightsAgent.postgres.statefulSet.extraEnv }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
          image: {{ include "langsmith.image" (dict "Values" .Values "Chart" .Chart "component" "postgresImage") | quote }}
          imagePullPolicy: {{ .Values.images.postgresImage.pullPolicy }}
          ports:
            - name: postgres
              containerPort: {{ .Values.engineInsightsAgent.postgres.containerPort }}
              protocol: TCP
          startupProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -d postgres -U postgres
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -d postgres -U postgres
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -d postgres -U postgres
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          resources:
            {{- toYaml .Values.engineInsightsAgent.postgres.statefulSet.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.engineInsightsAgent.postgres.statefulSet.securityContext | nindent 12 }}
          {{- with .Values.engineInsightsAgent.postgres.statefulSet.lifecycle }}
          lifecycle:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- with $volumeMounts }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.engineInsightsAgent.postgres.statefulSet.persistence.enabled }}
            - mountPath: /var/lib/postgresql/data
              name: data
              subPath: postgres
            {{- end }}
          {{- with .Values.engineInsightsAgent.postgres.statefulSet.extraContainerConfig }}
            {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.engineInsightsAgent.postgres.statefulSet.sidecars }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.engineInsightsAgent.postgres.statefulSet.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.engineInsightsAgent.postgres.statefulSet.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.engineInsightsAgent.postgres.statefulSet.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $volumes }}
      volumes:
          {{- toYaml . | nindent 8 }}
      {{- end }}
 {{- if .Values.engineInsightsAgent.postgres.statefulSet.persistence.enabled }}
  {{- with .Values.engineInsightsAgent.postgres.statefulSet.persistentVolumeClaimRetentionPolicy }}
  persistentVolumeClaimRetentionPolicy:
        {{- toYaml . | nindent 4 }}
  {{- end }}
  volumeClaimTemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: data
        labels:
          {{- include "langsmith.selectorLabels" . | nindent 10 }}
          app.kubernetes.io/component: {{ $componentName }}
      spec:

        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.engineInsightsAgent.postgres.statefulSet.persistence.size }}
        {{- if .Values.engineInsightsAgent.postgres.statefulSet.persistence.storageClassName }}
        storageClassName: {{ .Values.engineInsightsAgent.postgres.statefulSet.persistence.storageClassName }}
        {{- end }}
  {{- end }}
{{- end}}
{{- end }}
