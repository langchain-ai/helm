{{- if or .Values.insights.enabled .Values.engine.enabled }}
{{- if .Values.engineInsightsAgent.postgres.pdb.enabled }}
{{- $componentName := include "langsmith.agentFeatures.componentName" (dict "root" . "product" "engineInsightsAgent" "component" "postgres") -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ $componentName }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ $componentName }}
  {{- if hasKey .Values.engineInsightsAgent.postgres.pdb "minAvailable" }}
  minAvailable: {{ .Values.engineInsightsAgent.postgres.pdb.minAvailable }}
  {{- end }}
  {{- if hasKey .Values.engineInsightsAgent.postgres.pdb "maxUnavailable" }}
  maxUnavailable: {{ .Values.engineInsightsAgent.postgres.pdb.maxUnavailable }}
  {{- end }}
{{- end }}
{{- end }}
