{{- if or .Values.insights.enabled .Values.engine.enabled }}
{{- if not .Values.engineInsightsAgent.redis.external.enabled }}
{{- $componentName := include "langsmith.agentFeatures.componentName" (dict "root" . "product" "engineInsightsAgent" "component" "redis") -}}
{{- $volumes := concat .Values.commonVolumes .Values.engineInsightsAgent.redis.statefulSet.volumes -}}
{{- $volumeMounts := concat .Values.commonVolumeMounts .Values.engineInsightsAgent.redis.statefulSet.volumeMounts -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ $componentName }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.engineInsightsAgent.redis.statefulSet.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.engineInsightsAgent.redis.statefulSet.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  serviceName: {{ $componentName }}
  replicas: 1
  {{- with .Values.engineInsightsAgent.redis.statefulSet.updateStrategy }}
  updateStrategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ $componentName }}
  template:
    metadata:
      {{- with .Values.engineInsightsAgent.redis.statefulSet.annotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- with .Values.engineInsightsAgent.redis.statefulSet.labels }}
            {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "langsmith.labels" . | nindent 8 }}
        app.kubernetes.io/component: {{ $componentName }}
    spec:
      {{- with .Values.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.engineInsightsAgent.redis.statefulSet.priorityClassName }}
      priorityClassName: {{ .Values.engineInsightsAgent.redis.statefulSet.priorityClassName }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.engineInsightsAgent.redis.statefulSet.terminationGracePeriodSeconds }}
      securityContext:
        {{- toYaml .Values.engineInsightsAgent.redis.statefulSet.podSecurityContext | nindent 8 }}
      serviceAccountName: {{ $componentName }}
      {{- include "langsmith.dnsConfig" . | nindent 6 }}
      containers:
        - name: {{ .Values.engineInsightsAgent.redis.name }}
          {{- with .Values.engineInsightsAgent.redis.statefulSet.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if .Values.engineInsightsAgent.redis.statefulSet.extraEnv }}
          env:
            {{- toYaml .Values.engineInsightsAgent.redis.statefulSet.extraEnv | nindent 12 }}
          {{- end }}
          image: {{ include "langsmith.image" (dict "Values" .Values "Chart" .Chart "component" "redisImage") | quote }}
          imagePullPolicy: {{ .Values.images.redisImage.pullPolicy }}
          ports:
            - name: redis
              containerPort: {{ .Values.engineInsightsAgent.redis.containerPort }}
              protocol: TCP
          startupProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec redis-cli ping
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec redis-cli ping
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec redis-cli ping
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 1
          resources:
            {{- toYaml .Values.engineInsightsAgent.redis.statefulSet.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.engineInsightsAgent.redis.statefulSet.securityContext | nindent 12 }}
          {{- with .Values.engineInsightsAgent.redis.statefulSet.lifecycle }}
          lifecycle:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- with $volumeMounts }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.engineInsightsAgent.redis.statefulSet.persistence.enabled }}
            - mountPath: /data
              name: data
            {{- end }}
          {{- with .Values.engineInsightsAgent.redis.statefulSet.extraContainerConfig }}
            {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.engineInsightsAgent.redis.statefulSet.sidecars }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.engineInsightsAgent.redis.statefulSet.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.engineInsightsAgent.redis.statefulSet.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.engineInsightsAgent.redis.statefulSet.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $volumes }}
      volumes:
          {{- toYaml . | nindent 8 }}
      {{- end }}
 {{- if .Values.engineInsightsAgent.redis.statefulSet.persistence.enabled }}
  {{- with .Values.engineInsightsAgent.redis.statefulSet.persistentVolumeClaimRetentionPolicy }}
  persistentVolumeClaimRetentionPolicy:
        {{- toYaml . | nindent 4 }}
  {{- end }}
  volumeClaimTemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: data
        labels:
          {{- include "langsmith.selectorLabels" . | nindent 10 }}
          app.kubernetes.io/component: {{ $componentName }}
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.engineInsightsAgent.redis.statefulSet.persistence.size }}
        {{- if .Values.engineInsightsAgent.redis.statefulSet.persistence.storageClassName }}
        storageClassName: {{ .Values.engineInsightsAgent.redis.statefulSet.persistence.storageClassName }}
        {{- end }}
  {{- end }}
{{- end }}
{{- end }}
