{{- if or .Values.insights.enabled .Values.engine.enabled }}
{{- if not .Values.engineInsightsAgent.redis.external.existingSecretName }}
{{- $componentName := include "langsmith.agentFeatures.componentName" (dict "root" . "product" "engineInsightsAgent" "component" "redis") -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "langsmith.agentFeatures.redisSecretName" (dict "root" . "product" "engineInsightsAgent") }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
data:
  {{- if .Values.engineInsightsAgent.redis.external.enabled }}
  redis_connection_url: {{ .Values.engineInsightsAgent.redis.external.connectionUrl | b64enc }}
  {{- else }}
  redis_connection_url: {{ printf "redis://%s.%s.svc.%s:%v" $componentName (default .Release.Namespace .Values.namespace) .Values.clusterDomain (toString .Values.engineInsightsAgent.redis.service.port) | b64enc }}
  {{- end}}
{{- end }}
{{- end }}
