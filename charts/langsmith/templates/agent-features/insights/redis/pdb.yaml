{{- if .Values.insights.enabled }}
{{- if .Values.insights.redis.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "insights") }}-{{ .Values.insights.redis.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "insights") }}-{{ .Values.insights.redis.name }}
  {{- if .Values.insights.redis.pdb.minAvailable }}
  minAvailable: {{ .Values.insights.redis.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.insights.redis.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.insights.redis.pdb.maxUnavailable }}
  {{- end }}
{{- end }}
{{- end }}
