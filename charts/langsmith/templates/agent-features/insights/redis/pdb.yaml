{{- if .Values.insights.enabled }}
{{- if .Values.insights.redis.pdb.enabled }}
{{- $componentName := include "langsmith.agentFeatures.componentName" (dict "root" . "product" "insights" "component" "redis") -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ $componentName }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ $componentName }}
  {{- if hasKey .Values.insights.redis.pdb "minAvailable" }}
  minAvailable: {{ .Values.insights.redis.pdb.minAvailable }}
  {{- end }}
  {{- if hasKey .Values.insights.redis.pdb "maxUnavailable" }}
  maxUnavailable: {{ .Values.insights.redis.pdb.maxUnavailable }}
  {{- end }}
{{- end }}
{{- end }}
