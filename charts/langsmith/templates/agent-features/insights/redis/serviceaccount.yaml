{{- if or .Values.insights.enabled .Values.engine.enabled }}
{{- if and (not .Values.engineInsightsAgent.redis.external.enabled) .Values.engineInsightsAgent.redis.serviceAccount.create -}}
{{- $componentName := include "langsmith.agentFeatures.componentName" (dict "root" . "product" "engineInsightsAgent" "component" "redis") -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $componentName }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.engineInsightsAgent.redis.serviceAccount.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "langsmith.annotations" . | nindent 4 }}
    {{- with .Values.engineInsightsAgent.redis.serviceAccount.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
automountServiceAccountToken: {{ .Values.engineInsightsAgent.redis.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
