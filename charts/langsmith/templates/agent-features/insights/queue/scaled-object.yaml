{{- if or .Values.insights.enabled .Values.engine.enabled }}
{{- if and .Values.engineInsightsAgent.queue.enabled .Values.engineInsightsAgent.queue.autoscaling.enabled .Values.engineInsightsAgent.queue.autoscaling.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "engineInsightsAgent") }}-{{ .Values.engineInsightsAgent.queue.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "engineInsightsAgent") }}-{{ .Values.engineInsightsAgent.queue.name }}
  minReplicaCount: {{ .Values.engineInsightsAgent.queue.autoscaling.minReplicas | default 1 }}
  maxReplicaCount: {{ .Values.engineInsightsAgent.queue.autoscaling.maxReplicas | default 10 }}
  pollingInterval: {{ .Values.engineInsightsAgent.queue.autoscaling.keda.pollingInterval | default 30 }}
  cooldownPeriod: {{ .Values.engineInsightsAgent.queue.autoscaling.keda.cooldownPeriod | default 300 }}
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.engineInsightsAgent.queue.autoscaling.keda.scaleDownStabilizationWindowSeconds | default 300 }}
  triggers:
    {{- if .Values.engineInsightsAgent.queue.autoscaling.targetCPUUtilizationPercentage }}
    - type: cpu
      metricType: Utilization
      metadata:
        value: "{{ .Values.engineInsightsAgent.queue.autoscaling.targetCPUUtilizationPercentage }}"
    {{- end }}
    {{- if .Values.engineInsightsAgent.queue.autoscaling.targetMemoryUtilizationPercentage }}
    - type: memory
      metricType: Utilization
      metadata:
        value: "{{ .Values.engineInsightsAgent.queue.autoscaling.targetMemoryUtilizationPercentage }}"
    {{- end }}
    - type: postgresql
      metadata:
        connectionFromEnv: "POSTGRES_URI"
        query: "SELECT COUNT(*) FROM run WHERE status = 'pending'"
        targetQueryValue: {{ .Values.engineInsightsAgent.queue.numberOfJobsPerWorker | quote }}
        activationTargetQueryValue: {{ .Values.engineInsightsAgent.queue.numberOfJobsPerWorker | quote }}
    {{- with .Values.engineInsightsAgent.queue.autoscaling.keda.additionalTriggers }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
{{- end }}
