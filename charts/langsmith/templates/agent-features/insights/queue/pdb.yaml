{{- if or .Values.insights.enabled .Values.engine.enabled }}
{{- if .Values.engineInsightsAgent.queue.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "engineInsightsAgent") }}-{{ .Values.engineInsightsAgent.queue.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "langsmith.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "engineInsightsAgent") }}-{{ .Values.engineInsightsAgent.queue.name }}
  {{- if hasKey .Values.engineInsightsAgent.queue.pdb "minAvailable" }}
  minAvailable: {{ .Values.engineInsightsAgent.queue.pdb.minAvailable }}
  {{- end }}
  {{- if hasKey .Values.engineInsightsAgent.queue.pdb "maxUnavailable" }}
  maxUnavailable: {{ .Values.engineInsightsAgent.queue.pdb.maxUnavailable }}
  {{- end }}
{{- end }}
{{- end }}
