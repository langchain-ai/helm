{{- if or .Values.insights.enabled .Values.engine.enabled }}
{{- if and .Values.engineInsightsAgent.queue.enabled .Values.engineInsightsAgent.queue.autoscaling.enabled (not .Values.engineInsightsAgent.queue.autoscaling.keda.enabled) }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "engineInsightsAgent") }}-{{ .Values.engineInsightsAgent.queue.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "langsmith.agentFeatures.fullname" (dict "root" . "product" "engineInsightsAgent") }}-{{ .Values.engineInsightsAgent.queue.name }}
  minReplicas: {{ .Values.engineInsightsAgent.queue.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.engineInsightsAgent.queue.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.engineInsightsAgent.queue.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.engineInsightsAgent.queue.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.engineInsightsAgent.queue.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.engineInsightsAgent.queue.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
{{- end }}
