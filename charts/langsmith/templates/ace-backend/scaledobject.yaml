{{- if .Values.aceBackend.autoscaling.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.aceBackend.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.aceBackend.autoscaling.keda.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.aceBackend.autoscaling.keda.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    {{- if .Values.aceBackend.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.aceBackend.name }}
  pollingInterval: {{ .Values.aceBackend.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.aceBackend.autoscaling.keda.cooldownPeriod }}
  initialCooldownPeriod: {{ .Values.aceBackend.autoscaling.keda.initialCooldownPeriod }}
  minReplicaCount: {{ .Values.aceBackend.autoscaling.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.aceBackend.autoscaling.keda.maxReplicaCount }}
  fallback:
    {{- toYaml .Values.aceBackend.autoscaling.keda.fallback | nindent 4 }}
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.aceBackend.autoscaling.keda.scaleDownStabilizationWindowSeconds }}
          policies:
            - type: Percent
              value: {{ .Values.aceBackend.autoscaling.keda.scaleDownPolicy.value }}
              periodSeconds: {{ .Values.aceBackend.autoscaling.keda.scaleDownPolicy.periodSeconds }}
        scaleUp:
          policies:
            - type: Percent
              value: {{ .Values.aceBackend.autoscaling.keda.scaleUpPolicy.value }}
              periodSeconds: {{ .Values.aceBackend.autoscaling.keda.scaleUpPolicy.periodSeconds }}
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "{{ .Values.aceBackend.autoscaling.keda.targetCPUUtilizationPercentage }}"
    - type: memory
      metricType: Utilization
      metadata:
        value: "{{ .Values.aceBackend.autoscaling.keda.targetMemoryUtilizationPercentage }}"
{{- end }}
