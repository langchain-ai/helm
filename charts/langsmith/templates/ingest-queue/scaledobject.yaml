{{- if and .Values.ingestQueue.enabled .Values.ingestQueue.autoscaling.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.ingestQueue.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.ingestQueue.autoscaling.keda.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.ingestQueue.autoscaling.keda.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    {{- if .Values.ingestQueue.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.ingestQueue.name }}
  pollingInterval: {{ .Values.ingestQueue.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.ingestQueue.autoscaling.keda.cooldownPeriod }}
  initialCooldownPeriod: {{ .Values.ingestQueue.autoscaling.keda.initialCooldownPeriod }}
  minReplicaCount: {{ .Values.ingestQueue.autoscaling.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.ingestQueue.autoscaling.keda.maxReplicaCount }}
  fallback:
    {{- toYaml .Values.ingestQueue.autoscaling.keda.fallback | nindent 4 }}
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.ingestQueue.autoscaling.keda.scaleDownStabilizationWindowSeconds }}
          policies:
            - type: Percent
              value: {{ .Values.ingestQueue.autoscaling.keda.scaleDownPolicy.value }}
              periodSeconds: {{ .Values.ingestQueue.autoscaling.keda.scaleDownPolicy.periodSeconds }}
        scaleUp:
          policies:
            - type: Percent
              value: {{ .Values.ingestQueue.autoscaling.keda.scaleUpPolicy.value }}
              periodSeconds: {{ .Values.ingestQueue.autoscaling.keda.scaleUpPolicy.periodSeconds }}
  triggers:
    - type: metrics-api
      metadata:
        targetValue: "{{ .Values.ingestQueue.autoscaling.keda.queueTargetSize }}"
        url: "http://{{ include "langsmith-multitenant.fullname" . }}-{{ .Values.langsmith.platformBackend.name }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.langsmith.platformBackend.service.port }}/metrics?format=json"
        valueLocation: "ingestion_queue.pending"
        format: "json"
    - type: metrics-api
      metadata:
        targetValue: "{{ .Values.ingestQueue.autoscaling.keda.queueTargetSize }}"
        url: "http://{{ include "langsmith-multitenant.fullname" . }}-{{ .Values.langsmith.platformBackend.name }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.langsmith.platformBackend.service.port }}/metrics?format=json"
        valueLocation: "feedback_queue.pending"
        format: "json"
    - type: cpu
      metricType: Utilization
      metadata:
        value: "{{ .Values.ingestQueue.autoscaling.keda.targetCPUUtilizationPercentage }}"
    - type: memory
      metricType: Utilization
      metadata:
        value: "{{ .Values.ingestQueue.autoscaling.keda.targetMemoryUtilizationPercentage }}"
{{- end }}
