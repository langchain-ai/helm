{{- if and .Values.ingestQueue.enabled .Values.ingestQueue.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.ingestQueue.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    {{- if .Values.ingestQueue.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.ingestQueue.name }}
  minReplicas: {{ .Values.ingestQueue.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.ingestQueue.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.ingestQueue.autoscaling.hpa.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.ingestQueue.autoscaling.hpa.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.ingestQueue.autoscaling.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.ingestQueue.autoscaling.hpa.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
