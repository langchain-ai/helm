{{- if .Values.playground.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.playground.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    {{- if .Values.playground.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.playground.name }}
  minReplicas: {{ .Values.playground.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.playground.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.playground.autoscaling.hpa.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.playground.autoscaling.hpa.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.playground.autoscaling.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.playground.autoscaling.hpa.targetMemoryUtilizationPercentage }}
    {{- end }}
    {{- if .Values.playground.autoscaling.hpa.additionalMetrics }}
    {{- toYaml .Values.playground.autoscaling.hpa.additionalMetrics | nindent 4 }}
    {{- end }}
{{- end }}
