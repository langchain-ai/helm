{{- if .Values.playground.autoscaling.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.playground.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.playground.autoscaling.keda.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    scaledobject.keda.sh/transfer-hpa-ownership: "true"
    {{- with .Values.playground.autoscaling.keda.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    {{- if .Values.playground.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.playground.name }}
  pollingInterval: {{ .Values.playground.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.playground.autoscaling.keda.cooldownPeriod }}
  initialCooldownPeriod: {{ .Values.playground.autoscaling.keda.initialCooldownPeriod }}
  minReplicaCount: {{ .Values.playground.autoscaling.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.playground.autoscaling.keda.maxReplicaCount }}
  advanced:
    horizontalPodAutoscalerConfig:
      name: {{ include "langsmith.fullname" . }}-{{ .Values.playground.name }}
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.playground.autoscaling.keda.scaleDownStabilizationWindowSeconds }}
          policies:
            - type: Percent
              value: {{ .Values.playground.autoscaling.keda.scaleDownPolicy.value }}
              periodSeconds: {{ .Values.playground.autoscaling.keda.scaleDownPolicy.periodSeconds }}
        scaleUp:
          policies:
            - type: Percent
              value: {{ .Values.playground.autoscaling.keda.scaleUpPolicy.value }}
              periodSeconds: {{ .Values.playground.autoscaling.keda.scaleUpPolicy.periodSeconds }}
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "{{ .Values.playground.autoscaling.keda.targetCPUUtilizationPercentage }}"
    - type: memory
      metricType: Utilization
      metadata:
        value: "{{ .Values.playground.autoscaling.keda.targetMemoryUtilizationPercentage }}"
{{- end }}
