{{- if and .Values.config.deployment.enabled .Values.hostBackend.autoscaling.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.hostBackend.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.hostBackend.autoscaling.keda.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    scaledobject.keda.sh/transfer-hpa-ownership: "true"
    {{- with .Values.hostBackend.autoscaling.keda.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    {{- if .Values.hostBackend.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.hostBackend.name }}
  pollingInterval: {{ .Values.hostBackend.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.hostBackend.autoscaling.keda.cooldownPeriod }}
  initialCooldownPeriod: {{ .Values.hostBackend.autoscaling.keda.initialCooldownPeriod }}
  minReplicaCount: {{ .Values.hostBackend.autoscaling.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.hostBackend.autoscaling.keda.maxReplicaCount }}
  advanced:
    horizontalPodAutoscalerConfig:
      name: {{ include "langsmith.fullname" . }}-{{ .Values.hostBackend.name }}
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.hostBackend.autoscaling.keda.scaleDownStabilizationWindowSeconds }}
          policies:
            - type: Percent
              value: {{ .Values.hostBackend.autoscaling.keda.scaleDownPolicy.value }}
              periodSeconds: {{ .Values.hostBackend.autoscaling.keda.scaleDownPolicy.periodSeconds }}
        scaleUp:
          policies:
            - type: Percent
              value: {{ .Values.hostBackend.autoscaling.keda.scaleUpPolicy.value }}
              periodSeconds: {{ .Values.hostBackend.autoscaling.keda.scaleUpPolicy.periodSeconds }}
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "{{ .Values.hostBackend.autoscaling.keda.targetCPUUtilizationPercentage }}"
    - type: memory
      metricType: Utilization
      metadata:
        value: "{{ .Values.hostBackend.autoscaling.keda.targetMemoryUtilizationPercentage }}"
{{- end }}
