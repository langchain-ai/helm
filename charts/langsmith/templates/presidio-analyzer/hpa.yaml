{{- if and .Values.presidioAnalyzer.enabled .Values.presidioAnalyzer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.presidioAnalyzer.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "langsmith.fullname" . }}-{{ .Values.presidioAnalyzer.name }}
  minReplicas: {{ .Values.presidioAnalyzer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.presidioAnalyzer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.presidioAnalyzer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.presidioAnalyzer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
{{- end }}
