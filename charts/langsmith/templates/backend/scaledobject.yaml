{{- if .Values.backend.autoscaling.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.backend.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.backend.autoscaling.keda.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.backend.autoscaling.keda.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    {{- if .Values.backend.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.backend.name }}
  pollingInterval: {{ .Values.backend.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.backend.autoscaling.keda.cooldownPeriod }}
  initialCooldownPeriod: {{ .Values.backend.autoscaling.keda.initialCooldownPeriod }}
  minReplicaCount: {{ .Values.backend.autoscaling.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.backend.autoscaling.keda.maxReplicaCount }}
  fallback:
    {{- toYaml .Values.backend.autoscaling.keda.fallback | nindent 4 }}
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.backend.autoscaling.keda.scaleDownStabilizationWindowSeconds }}
          policies:
            - type: Percent
              value: {{ .Values.backend.autoscaling.keda.scaleDownPolicy.value }}
              periodSeconds: {{ .Values.backend.autoscaling.keda.scaleDownPolicy.periodSeconds }}
        scaleUp:
          policies:
            - type: Percent
              value: {{ .Values.backend.autoscaling.keda.scaleUpPolicy.value }}
              periodSeconds: {{ .Values.backend.autoscaling.keda.scaleUpPolicy.periodSeconds }}
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "{{ .Values.backend.autoscaling.keda.targetCPUUtilizationPercentage }}"
    - type: memory
      metricType: Utilization
      metadata:
        value: "{{ .Values.backend.autoscaling.keda.targetMemoryUtilizationPercentage }}"
{{- end }}
