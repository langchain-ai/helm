{{- if .Values.frontend.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.frontend.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    {{- if .Values.frontend.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.frontend.name }}
  minReplicas: {{ .Values.frontend.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.frontend.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.frontend.autoscaling.hpa.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.frontend.autoscaling.hpa.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.frontend.autoscaling.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.frontend.autoscaling.hpa.targetMemoryUtilizationPercentage }}
    {{- end }}
    {{- if .Values.frontend.autoscaling.hpa.additionalMetrics }}
    {{- toYaml .Values.frontend.autoscaling.hpa.additionalMetrics | nindent 4 }}
    {{- end }}
{{- end }}
