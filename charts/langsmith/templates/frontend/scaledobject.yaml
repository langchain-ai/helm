{{- if .Values.frontend.autoscaling.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "langsmith.fullname" . }}-{{ .Values.frontend.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "langsmith.labels" . | nindent 4 }}
    {{- with .Values.frontend.autoscaling.keda.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.frontend.autoscaling.keda.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    {{- if .Values.frontend.rollout.enabled }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ include "langsmith.fullname" . }}-{{ .Values.frontend.name }}
  pollingInterval: {{ .Values.frontend.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.frontend.autoscaling.keda.cooldownPeriod }}
  initialCooldownPeriod: {{ .Values.frontend.autoscaling.keda.initialCooldownPeriod }}
  minReplicaCount: {{ .Values.frontend.autoscaling.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.frontend.autoscaling.keda.maxReplicaCount }}
  fallback:
    {{- toYaml .Values.frontend.autoscaling.keda.fallback | nindent 4 }}
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.frontend.autoscaling.keda.scaleDownStabilizationWindowSeconds }}
          policies:
            - type: Percent
              value: {{ .Values.frontend.autoscaling.keda.scaleDownPolicy.value }}
              periodSeconds: {{ .Values.frontend.autoscaling.keda.scaleDownPolicy.periodSeconds }}
        scaleUp:
          policies:
            - type: Percent
              value: {{ .Values.frontend.autoscaling.keda.scaleUpPolicy.value }}
              periodSeconds: {{ .Values.frontend.autoscaling.keda.scaleUpPolicy.periodSeconds }}
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "{{ .Values.frontend.autoscaling.keda.targetCPUUtilizationPercentage }}"
    - type: memory
      metricType: Utilization
      metadata:
        value: "{{ .Values.frontend.autoscaling.keda.targetMemoryUtilizationPercentage }}"
{{- end }}
