config:
  sandboxes:
    sandboxHost:
      deployment:
        nodeSelector:
          sandbox.example.com/pool: hosts
        tolerations:
          - key: sandbox.example.com/pool
            operator: Equal
            value: hosts
            effect: NoSchedule
