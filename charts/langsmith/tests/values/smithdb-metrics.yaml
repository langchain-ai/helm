smithdb:
  config:
    observability:
      metrics:
        enabled: true
        annotations:
          "example.com/{{ .containerName }}": "{{ .component }}:{{ .metricsPort }}{{ .metricsPath }}"
  query:
    deployment:
      annotations:
        prometheus.io/scrape: "false"
