{{- if .Values.istioGateway.enabled }}
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ include "langGraphCloud.fullname" . }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  annotations:
    {{- include "langGraphCloud.annotations" . | nindent 4 }}
    {{- with .Values.istioGateway.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  labels:
    {{- include "langGraphCloud.labels" . | nindent 4 }}
    {{- with .Values.istioGateway.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.istioGateway.hostname }}
  hosts:
  - {{ .Values.istioGateway.hostname | quote }}
  {{- else }}
  hosts:
  - "*"
  {{- end }}
  gateways:
  {{- if .Values.istioGateway.namespace }}
  - {{ .Values.istioGateway.namespace }}/{{ .Values.istioGateway.name }}
  {{- else }}
  - {{ .Values.istioGateway.name }}
  {{- end }}
  {{- $bp := .Values.istioGateway.basePath | trimPrefix "/" | trimSuffix "/" }}
  {{- $apiHost := printf "%s-%s.%s.svc.%s" (include "langGraphCloud.fullname" .) .Values.apiServer.name (.Values.namespace | default .Release.Namespace) .Values.clusterDomain }}
  http:
  {{- if $bp }}
  - match:
    - uri:
        exact: /{{ $bp }}
    rewrite:
      uri: /
    route:
    - destination:
        host: {{ $apiHost }}
        port:
          number: {{ .Values.apiServer.service.httpPort }}
  - match:
    - uri:
        prefix: /{{ $bp }}/
    rewrite:
      uri: /
    route:
    - destination:
        host: {{ $apiHost }}
        port:
          number: {{ .Values.apiServer.service.httpPort }}
  {{- else }}
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ $apiHost }}
        port:
          number: {{ .Values.apiServer.service.httpPort }}
  {{- end }}
{{- if and .Values.studio.enabled .Values.istioGateway.studioHostname }}
---
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ include "langGraphCloud.fullname" . }}-studio
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  annotations:
    {{- include "langGraphCloud.annotations" . | nindent 4 }}
    {{- with .Values.istioGateway.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  labels:
    {{- include "langGraphCloud.labels" . | nindent 4 }}
    {{- with .Values.istioGateway.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  hosts:
  - {{ .Values.istioGateway.studioHostname | quote }}
  gateways:
  {{- if .Values.istioGateway.namespace }}
  - {{ .Values.istioGateway.namespace }}/{{ .Values.istioGateway.name }}
  {{- else }}
  - {{ .Values.istioGateway.name }}
  {{- end }}
  {{- $studioHost := printf "%s-%s.%s.svc.%s" (include "langGraphCloud.fullname" .) .Values.studio.name (.Values.namespace | default .Release.Namespace) .Values.clusterDomain }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ $studioHost }}
        port:
          number: {{ .Values.studio.service.httpPort }}
{{- end }}
{{- end }}
