{{- $defaultCheckpointerBackend := include "langGraphCloud.defaultCheckpointerBackend" . -}}
{{- if and (eq $defaultCheckpointerBackend "mongo") (not .Values.checkpointer.default.mongo.existingSecretName) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "langGraphCloud.mongoSecretsName" . }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langGraphCloud.labels" . | nindent 4 }}
  annotations:
    {{- include "langGraphCloud.annotations" . | nindent 4 }}
data:
  {{ .Values.checkpointer.default.mongo.connectionUrlSecretKey }}: {{ .Values.checkpointer.default.mongo.connectionUrl | b64enc | quote }}
{{- end }}
