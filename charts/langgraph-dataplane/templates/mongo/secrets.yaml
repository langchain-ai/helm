{{- include "langgraphDataplane.validateMongoConfiguration" . -}}
{{- $useExistingSecret := and .Values.mongo.external.enabled .Values.mongo.external.existingSecretName -}}
{{- if and .Values.mongo.enabled (not $useExistingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "langgraphDataplane.mongoSecretsName" . }}
  namespace: {{ .Values.namespace | default .Release.Namespace | quote }}
  labels:
    {{- include "langgraphDataplane.labels" . | nindent 4 }}
  annotations:
    {{- include "langgraphDataplane.annotations" . | nindent 4 }}
data:
  mongodb_connection_url: {{ include "langgraphDataplane.mongoConnectionUrl" . | b64enc | quote }}
{{- end }}
