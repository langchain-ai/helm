suite: ServiceAccount token automount configuration
templates:
  - backend/service-account.yaml
tests:
  - it: configures the backend ServiceAccount
    set:
      backend.serviceAccount.automountServiceAccountToken: false
    asserts:
      - equal:
          path: automountServiceAccountToken
          value: false
