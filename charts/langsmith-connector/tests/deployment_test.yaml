suite: connector deployment
templates:
  - deployment.yaml
values:
  - common-values.yaml
tests:
  - it: renders the four LangSmith env vars from values
    set:
      connector.endpoint: "https://langsmith.example.com/api"
      connector.targets:
        database: "db.internal.example:5432"
        api: "https://api.internal.example:8443"
    asserts:
      - isKind:
          of: Deployment
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: LANGSMITH_ENDPOINT
            value: "https://langsmith.example.com/api"
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: LANGSMITH_CONNECTION_ID
            value: "0f7e5f1c-1b2d-4c3e-8a9b-0c1d2e3f4a5b"
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: LANGSMITH_API_KEY_FILE
            value: /var/run/secrets/langsmith/api-key
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: LANGSMITH_CONNECTOR_TARGETS
            value: "api=https://api.internal.example:8443,database=db.internal.example:5432"

  - it: mounts the chart-managed secret as the api-key file
    asserts:
      - equal:
          path: spec.template.spec.volumes[0].secret.secretName
          value: RELEASE-NAME-langsmith-connector
      - equal:
          path: spec.template.spec.volumes[0].secret.items[0].key
          value: api-key
      - equal:
          path: spec.template.spec.containers[0].volumeMounts[0].mountPath
          value: /var/run/secrets/langsmith
      - equal:
          path: spec.template.spec.containers[0].volumeMounts[0].readOnly
          value: true

  - it: mounts an existing secret with its configured key
    set:
      connector.apiKey: ""
      connector.existingSecret: "my-langsmith"
      connector.existingSecretKey: "LANGSMITH_API_KEY"
    asserts:
      - equal:
          path: spec.template.spec.volumes[0].secret.secretName
          value: my-langsmith
      - equal:
          path: spec.template.spec.volumes[0].secret.items[0].key
          value: LANGSMITH_API_KEY
      - equal:
          path: spec.template.spec.volumes[0].secret.items[0].path
          value: api-key

  - it: exposes no ports and defines no probes
    asserts:
      - notExists:
          path: spec.template.spec.containers[0].ports
      - notExists:
          path: spec.template.spec.containers[0].livenessProbe
      - notExists:
          path: spec.template.spec.containers[0].readinessProbe

  - it: runs hardened and without a service account token
    asserts:
      - equal:
          path: spec.template.spec.automountServiceAccountToken
          value: false
      - equal:
          path: spec.template.spec.securityContext.runAsNonRoot
          value: true
      - equal:
          path: spec.template.spec.containers[0].securityContext.readOnlyRootFilesystem
          value: true
      - equal:
          path: spec.template.spec.containers[0].securityContext.allowPrivilegeEscalation
          value: false
      - equal:
          path: spec.template.spec.terminationGracePeriodSeconds
          value: 45

  - it: uses the image from values with the registry prefix
    set:
      images.registry: "registry.example.com"
    asserts:
      - equal:
          path: spec.template.spec.containers[0].image
          value: "registry.example.com/docker.io/langchain/langsmith-connector:0.1.0"

  - it: uses the default image without a registry prefix
    asserts:
      - equal:
          path: spec.template.spec.containers[0].image
          value: "docker.io/langchain/langsmith-connector:0.1.0"

  - it: rolls pods without surging
    asserts:
      - equal:
          path: spec.strategy.rollingUpdate.maxSurge
          value: 0
      - equal:
          path: spec.strategy.rollingUpdate.maxUnavailable
          value: 1

  - it: appends extraEnv after the LangSmith variables
    set:
      connector.extraEnv:
        - name: FOO
          value: bar
    asserts:
      - equal:
          path: spec.template.spec.containers[0].env[4].name
          value: FOO

  - it: uses the default service account when creation is disabled
    set:
      serviceAccount.create: false
    asserts:
      - equal:
          path: spec.template.spec.serviceAccountName
          value: default

  - it: never places the api key in the deployment
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            name: LANGSMITH_API_KEY
          any: true
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            value: test-api-key
          any: true

  - it: sets runAsUser and runAsGroup 1000
    asserts:
      - equal:
          path: spec.template.spec.securityContext.runAsUser
          value: 1000
      - equal:
          path: spec.template.spec.securityContext.runAsGroup
          value: 1000

  - it: renders pod annotations only when configured
    asserts:
      - notExists:
          path: spec.template.metadata.annotations

  - it: renders pod annotations when commonPodAnnotations is set
    set:
      commonPodAnnotations.foo: bar
    asserts:
      - equal:
          path: spec.template.metadata.annotations.foo
          value: bar
